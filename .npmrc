# BatchTranslate pnpm 配置（架构第 1、3 节技术栈：WXT + Preact + TS）
# 本地与 CI 一致：自动安装 peer 依赖，避免 WXT/Preact/Vitest 间 peer 漂移导致 install 失败。
auto-install-peers=true
strict-peer-dependencies=false
# CI 日志可读：不强制 hoist，保持 pnpm 严格隔离（默认 isolated，确保扩展打包无幻影依赖）。
node-linker=isolated
